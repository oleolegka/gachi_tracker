# build output
build/
.gradle/
.kotlin/

# local environment (everyone has their own SDK path)
local.properties

# artifacts and keys — these must never reach the repository
*.apk
*.aab
*.jks
*.keystore

# IDE
.idea/
*.iml
.DS_Store

# agent worktrees and local tooling
.claude/
