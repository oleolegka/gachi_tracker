<?xml version="1.0" encoding="utf-8"?>
<!--
    The only thing the app ever shares: a program export written into cache/exports.
    Nothing else is reachable through the provider - not the database, not the
    preferences, not the rest of the cache.
-->
<paths>
    <cache-path
        name="exports"
        path="exports/" />
</paths>
